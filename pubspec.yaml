name: duration_spinbox
description: A duration widget for Flutter, heavily inspired by flutter_spinbox
version: 0.1.0+1


environment:
  sdk: ^3.4.0
  flutter: ^3.22.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.3
  very_good_analysis: ">=5.1.0 <7.0.0"
